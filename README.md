# SECURE-FILE-STORAGE-SYSTEM
Hybrid RSA/AES CLI: per-user RSA keypairs and AES-128-CBC file encryption with unique AES session keys protected by RSA. Implements PKCS#7 padding, CBC mode, Miller–Rabin prime generation, key schedule, S-boxes, and modular arithmetic manually. Educational, not production-ready.
