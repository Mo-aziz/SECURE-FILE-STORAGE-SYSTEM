
import os

# AES S-box (standard from NIST FIPS 197)
SBOX = [
    ,
]

# Inverse S-box (standard from NIST FIPS 197)
SBOX_INV = [
    ,
]

RCON = [0x01, 0x02, 0x04, 0x08, 0x10, 0x20, 0x40, 0x80, 0x1b, 0x36]

def gmul(a, b):
    
    p = 0
    for _ in range(8):
        if b & 1:
            p ^= a
        hi = a & 0x80
        a <<= 1
        a &= 0xFF
        if hi:
            a ^= 0x1b
        b >>= 1
    return p

def pad_pkcs7(data: bytes, block_size: int = 16) -> bytes:
  
    n = block_size - (len(data) % block_size)
    return data + bytes([n] * n)

def unpad_pkcs7(data: bytes) -> bytes:
  
    return data[:-data[-1]]

def key_expansion(key: bytes) -> list:
   
    assert len(key) == 16
    w = []
    for i in range(4):
        w.append(int.from_bytes(key[4*i:4*i+4], 'big'))
    for i in range(4, 44):
        tmp = w[i-1]
        if i % 4 == 0:
            tmp = ((tmp << 8) | (tmp >> 24)) & 0xffffffff
            tmp = (SBOX[(tmp >> 24) & 0xff] << 24 |
                   SBOX[(tmp >> 16) & 0xff] << 16 |
                   SBOX[(tmp >> 8) & 0xff] << 8 |
                   SBOX[tmp & 0xff])
            tmp ^= RCON[(i // 4) - 1] << 24
        w.append(w[i - 4] ^ tmp)
    rk = []
    for i in range(0, 44, 4):
        rk.append(b''.join(w[i+j].to_bytes(4, 'big') for j in range(4)))
    return [list(k) for k in rk]

def aes_encrypt_block(pt: bytes, rk: list) -> bytes:
   
    assert len(pt) == 16
    s = [[pt[r + 4*c] for c in range(4)] for r in range(4)]
    # Initial AddRoundKey (key whitening)
    for i in range(4):
        for j in range(4):
            s[i][j] ^= rk[0][i + 4*j]  # Column=major indexing
    # 9 main rounds
    for round_num in range(1, 10):
        # SubBytes
        for i in range(4):
            for j in range(4):
                s[i][j] = SBOX[s[i][j]]
        # ShiftRows
        s[1] = s[1][1:] + s[1][:1]
        s[2] = s[2][2:] + s[2][:2]
        s[3] = s[3][3:] + s[3][:3]
        # MixColumns
        for c in range(4):
            col = [s[r][c] for r in range(4)]
            s[0][c] = gmul(2, col[0]) ^ gmul(3, col[1]) ^ col[2] ^ col[3]
            s[1][c] = col[0] ^ gmul(2, col[1]) ^ gmul(3, col[2]) ^ col[3]
            s[2][c] = col[0] ^ col[1] ^ gmul(2, col[2]) ^ gmul(3, col[3])
            s[3][c] = gmul(3, col[0]) ^ col[1] ^ col[2] ^ gmul(2, col[3])
        # AddRoundKey
        for i in range(4):
            for j in range(4):
                s[i][j] ^= rk[round_num][i + 4*j]  # Column-major indexing
    # Round 10 (final round, no MixColumns)
    for i in range(4):
        for j in range(4):
            s[i][j] = SBOX[s[i][j]]
    s[1] = s[1][1:] + s[1][:1]
    s[2] = s[2][2:] + s[2][:2]
    s[3] = s[3][3:] + s[3][:3]
    # Final AddRoundKey
    for i in range(4):
        for j in range(4):
            s[i][j] ^= rk[10][i + 4*j]  # Column major indexing
    return b''.join(bytes([s[r][c]]) for c in range(4) for r in range(4))

def aes_decrypt_block(ct: bytes, rk: list) -> bytes:
    """Decrypt one 128-bit block."""
    assert len(ct) == 16
    s = [[ct[r + 4*c] for c in range(4)] for r in range(4)]
    # Initial AddRoundKey with last round key
    for i in range(4):
        for j in range(4):
            s[i][j] ^= rk[10][i + 4*j]  # Column major indexing
    # 9 inverse rounds (in reverse order)
    for round_num in range(9, 0, -1):
        # InvShiftRows (inverse of ShiftRows - rotate right instead of left)
        s[1] = s[1][-1:] + s[1][:-1]
        s[2] = s[2][-2:] + s[2][:-2]
        s[3] = s[3][-3:] + s[3][:-3]
        # InvSubBytes
        for i in range(4):
            for j in range(4):
                s[i][j] = SBOX_INV[s[i][j]]
        # AddRoundKey
        for i in range(4):
            for j in range(4):
                s[i][j] ^= rk[round_num][i + 4*j]  # Column major indexing
        # InvMixColumns
        for c in range(4):
            col = [s[r][c] for r in range(4)]
            s[0][c] = gmul(0x0e, col[0]) ^ gmul(0x0b, col[1]) ^ gmul(0x0d, col[2]) ^ gmul(0x09, col[3])
            s[1][c] = gmul(0x09, col[0]) ^ gmul(0x0e, col[1]) ^ gmul(0x0b, col[2]) ^ gmul(0x0d, col[3])
            s[2][c] = gmul(0x0d, col[0]) ^ gmul(0x09, col[1]) ^ gmul(0x0e, col[2]) ^ gmul(0x0b, col[3])
            s[3][c] = gmul(0x0b, col[0]) ^ gmul(0x0d, col[1]) ^ gmul(0x09, col[2]) ^ gmul(0x0e, col[3])
    # Final round (no InvMixColumns)
    s[1] = s[1][-1:] + s[1][:-1]
    s[2] = s[2][-2:] + s[2][:-2]
    s[3] = s[3][-3:] + s[3][:-3]
    for i in range(4):
        for j in range(4):
            s[i][j] = SBOX_INV[s[i][j]]
    # Final AddRoundKey with first round key
    for i in range(4):
        for j in range(4):
            s[i][j] ^= rk[0][i + 4*j]  # Column-major indexing
    return b''.join(bytes([s[r][c]]) for c in range(4) for r in range(4))

def aes_cbc_encrypt(pt: bytes, key: bytes, iv: bytes = None) -> tuple:
    """Encrypt with AES-128-CBC."""
    if iv is None:
        iv = os.urandom(16)
    assert len(key) == 16 and len(iv) == 16
    pt = pad_pkcs7(pt)
    rk = key_expansion(key)
    ct = b''
    prev = iv
    for i in range(0, len(pt), 16):
        blk = bytes(pt[j + i] ^ prev[j] for j in range(16))
        enc = aes_encrypt_block(blk, rk)
        ct += enc
        prev = enc
    return ct, iv

def aes_cbc_decrypt(ct: bytes, key: bytes, iv: bytes) -> bytes:
    """Decrypt with AES-128-CBC."""
    assert len(key) == 16 and len(iv) == 16 and len(ct) % 16 == 0
    rk = key_expansion(key)
    pt = b''
    prev = iv
    for i in range(0, len(ct), 16):
        blk = ct[i:i+16]
        dec = aes_decrypt_block(blk, rk)
        xor = bytes(dec[j] ^ prev[j] for j in range(16))
        pt += xor
        prev = blk
    return unpad_pkcs7(pt)
